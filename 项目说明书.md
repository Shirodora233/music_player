# FPGA 音乐播放器项目说明书

## 1. 项目概述

本项目是在 Vivado 2018.1 工程 `lab_music` 上实现的 FPGA 简易音乐播放器。系统可以播放内置乐曲，通过蜂鸣器输出方波音频，并使用 32 个独立 LED 和 8 位数码管显示当前音符、八度、升降号、节拍、播放进度、播放时间和当前可编辑参数。

工程入口为：

```text
lab_music/lab_music.xpr
```

顶层模块为：

```text
lab_music/lab_music.srcs/sources_1/new/music_top.v
```

当前 FPGA 型号和工程器件设置为 `xc7k325tffg900-2`。约束文件为：

```text
lab_music/lab_music.srcs/constrs_1/new/music.xdc
```

## 2. 硬件资源分配

### 2.1 时钟、按键和蜂鸣器

| 功能 | 板上资源 | FPGA 引脚 | 有效电平 |
| --- | --- | --- | --- |
| 系统时钟 | CLK | AD12 | 200 MHz |
| 复位 | KEY8 | D11 | 低有效 |
| 播放/暂停 | KEY7 | C11 | 低有效 |
| 停止 | KEY6 | E14 | 低有效 |
| 参数选择 | KEY5 | B13 | 低有效 |
| 参数减小 | KEY4 | A13 | 低有效 |
| 参数增大 | KEY3 | D14 | 低有效 |
| 蜂鸣器 | BEEP | W19 | 方波输出 |

代码中仍保留了历史端口名 `key_volume_down` 和 `key_volume_up`，但当前含义已经改为“参数减小”和“参数增大”。

### 2.2 32 个独立 LED

LED 按板子照片中的物理位置抽象为四行，每行 8 个灯，从左到右编号为 bit0 到 bit7。实际原理图 LED 编号由 `board_led_mapper.v` 负责转换。

| 逻辑行 | 显示内容 |
| --- | --- |
| 第 0 行 | 当前音名，前 7 个灯依次表示 C、D、E、F、G、A、B |
| 第 1 行 | 当前八度，0 八度全灭，1 到 8 八度分别点亮一个灯 |
| 第 2 行 | bit0 表示降号，bit1 表示升号，bit7 用于节拍闪烁 |
| 第 3 行 | 当前乐曲播放进度条 |

### 2.3 8 位数码管

数码管由 4 个两位数码管模块组成。代码中把它抽象为 8 个逻辑位，从右到左编号为 `digit0` 到 `digit7`：

| 数码管区域 | 显示内容 |
| --- | --- |
| 左边四位 | 当前正在编辑或默认显示的参数 |
| 右边四位 | 当前乐曲播放时间，格式为 `MM.SS` |

左四位显示格式：

| 参数 | 显示例子 | 说明 |
| --- | --- | --- |
| 乐曲编号 | `S001`、`S002` | 显示为 1 开始编号，内部仍是 0 开始 |
| 移调 | `t+00`、`t-12` | 范围为 -12 到 +12 半音 |
| BPM | `b120` | 范围为 30 到 250 |
| 音量 | `U007` | 范围为 0 到 7，0 为静音 |

暂停时，右侧四位时间显示会闪烁；左侧参数显示不闪烁。

## 3. 操作说明

1. 按 KEY8 复位系统。
2. 按 KEY7 开始播放；播放中再次按 KEY7 暂停；暂停时再按 KEY7 继续播放。
3. 按 KEY6 停止播放，并回到当前乐曲的第一个音。
4. 按 KEY5 在“乐曲编号、移调、BPM、音量”四个参数之间循环选择。
5. 按 KEY4 减小当前参数，按 KEY3 增大当前参数。
6. 如果 5 秒没有进行参数操作，显示会自动回到乐曲编号模式。

切换乐曲时，播放器会自动停止并把音符索引清零。

## 4. 当前内置乐曲

| 显示编号 | 内部编号 | 乐曲 |
| --- | --- | --- |
| `S001` | 0 | Twinkle Twinkle Little Star |
| `S002` | 1 | Two Tigers |

当前两首曲子的默认 BPM 都是 120，调性元数据为 C 大调。

## 5. 显示现象参考

系统复位后，默认乐曲为 `S001`，移调为 `t+00`，BPM 为 `b120`。当前音符从第一首歌第一个音开始，因此 LED 音名行会显示 C，八度行会显示第 4 八度。
默认音量为 `U007`。音量为 0 时静音，1 到 7 档使用逐步增大的蜂鸣器高电平占空比。

播放时：

- 蜂鸣器输出当前音高对应的方波。
- 每个音根据自身时值持续播放。
- 第三行最后一个 LED 按四分音符节拍短暂闪烁。
- 最后一行 LED 根据整首歌的十六分音符进度逐步点亮。
- 右侧数码管显示经过时间。

## 6. 仿真和验证

已经用 Vivado 2018.1 自带的 `xvlog/xelab/xsim` 跑通过测试平台，输出为：

```text
PASS: music player control, tone, pitch, and UI tests completed
```

如果不把 Vivado 加入 PATH，可以在 PowerShell 中进入 `lab_music` 目录后使用完整路径运行：

```powershell
& 'D:\ProgramFiles\vivado\Vivado\2018.1\bin\xvlog.bat' lab_music.srcs\sources_1\new\button_debounce.v lab_music.srcs\sources_1\new\ui_controller.v lab_music.srcs\sources_1\new\player_controller.v lab_music.srcs\sources_1\new\song_rom.v lab_music.srcs\sources_1\new\pitch_processor.v lab_music.srcs\sources_1\new\beat_controller.v lab_music.srcs\sources_1\new\tone_generator.v lab_music.srcs\sources_1\new\led_panel_controller.v lab_music.srcs\sources_1\new\board_led_mapper.v lab_music.srcs\sources_1\new\sevenseg_scan_controller.v lab_music.srcs\sources_1\new\sevenseg_ui_formatter.v lab_music.srcs\sources_1\new\music_top.v lab_music.srcs\sim_1\new\tb_music_top.v
& 'D:\ProgramFiles\vivado\Vivado\2018.1\bin\xelab.bat' tb_music_top -s tb_music_top_sim
& 'D:\ProgramFiles\vivado\Vivado\2018.1\bin\xsim.bat' tb_music_top_sim -runall
```

Vivado 仿真会生成 `xsim.dir`、日志和数据库文件，这些属于临时文件，不需要提交到 Git。

## 7. 上板注意事项

由于当前没有实物板进行最终确认，以下硬件对应关系需要上板后重点观察：

1. 32 个独立 LED 的物理行列方向是否与照片一致。
2. 数码管是否为段线高有效、位选低有效。
3. `LED3/LED4` 两个双位数码管的左右位选是否需要互换。
4. 如果数码管某些段显示错位，应优先检查 XDC 中 `seg[n]` 的 A/B/C/D/E/F/G/DP 顺序。
5. 如果数码管只显示一位或完全不亮，应优先检查 `seg_cs[n]` 的有效电平和左右位选顺序。

上述问题通常只需要修改 `music.xdc` 或 `sevenseg_scan_controller.v` 的极性参数，不需要改动歌曲、移调或播放器核心逻辑。
